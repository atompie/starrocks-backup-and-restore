[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "starrocks-br"
version = "0.7.0a1"
description = "StarRocks Backup and Restore automation tool"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mysql-connector-python>=9.0.0,<10",
    "fastapi>=0.115.0,<1",
    "uvicorn>=0.30.0,<1",
    "sqlalchemy>=2.0.0,<3",
    "alembic>=1.13.0,<2",
    "httpx>=0.27.0,<1",
    "croniter>=2.0.0,<3",
    "cryptography>=42.0.0,<44",
    "pydantic>=2.7.0,<3",
    "boto3>=1.34.0,<2",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.3.2,<9",
    "pytest-mock>=3.14.0,<4",
    "pytest-cov>=5.0.0,<6",
    "ruff>=0.8.0,<1",
    "pre-commit>=4.0.0,<5",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
addopts = "-q --cov=src/starrocks_br --cov-report=term-missing"
testpaths = ["tests"]

[tool.coverage.run]
branch = true
source = ["src/starrocks_br"]

[tool.coverage.report]
omit = ["*/__init__.py"]
show_missing = true
skip_covered = false

[tool.black]
line-length = 100

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = [
    "E",    # pycodestyle errors
    "F",    # pyflakes (unused imports, variables, etc.)
    "I",    # isort (import sorting)
    "N",    # pep8-naming
    "UP",   # pyupgrade (modernize Python code)
    "B",    # flake8-bugbear (likely bugs)
    "C4",   # flake8-comprehensions
    "ARG",  # flake8-unused-arguments (unused function parameters)
]
ignore = [
    "E501",  # Line too long (let Black handle line length)
]

[tool.ruff.lint.per-file-ignores]
"__init__.py" = ["F401"]  # Allow unused imports in __init__.py files
"tests/**" = ["ARG001", "ARG005"]   # Allow unused arguments in tests (pytest fixtures, stub handlers)
"src/starrocks_br/api/**" = ["B008"]  # FastAPI's Depends(...) default-argument idiom
