## 1. Align Scheduled Backup Inputs

- [x] 1.1 Add repository to persisted schedule and API schemas, including an Alembic migration and a safe rollout policy for existing schedules; verify migration upgrade and downgrade behavior. (Already implemented by prior work: `Schedule.repository` on the model, migration `d96bf30a5582_add_schedule_repository.py` — clean cutover, no data migration needed since no production rows existed. Verified `alembic upgrade head` → `downgrade -1` → `upgrade head` round-trips cleanly against a scratch sqlite db.)
- [x] 1.2 Preserve and validate repository through schedule create, update, and read flows; verify schedule API behavior matches the existing scheduling contract. (Already implemented: `ScheduleCreate`/`ScheduleUpdate`/`ScheduleRead` all carry `repository`; create/update validate it against the cluster via `ensure_repository_exists`. Confirmed against `openspec/specs/api-scheduling/spec.md` and existing `tests/test_api_schedules.py` coverage — all scenarios in the spec are exercised.)

## 2. Unify Backup Command Dispatch

- [x] 2.1 Ensure manual full and incremental submissions and due-schedule jobs carry equivalent group, repository, and type-specific parameters into the existing per-type backup commands; verify command handler dispatch for both job types. (Already implemented: manual routes build params via `payload.model_dump(exclude={"backend"})`; `run_due_schedules` builds `{"group_id": ..., "repository": ...}`; both submit through `commands.jobs.submit_job` and dispatch via the single `jobs.handlers.JOB_HANDLERS` map to `commands.backup.run_backup_full`/`run_backup_incremental`. Verified with new parity tests — see 3.1.)
- [x] 2.2 Keep route-specific validation and asynchronous submission intact while removing any duplicate backup execution logic; verify existing manual job response and error behavior remains unchanged. (Already implemented: `api/routes/jobs.py` keeps synchronous group/repository pre-validation and HTTP error translation; `jobs/handlers.py` is a pure dispatch table with no backup logic of its own, per its own module docstring. `tests/test_api_jobs.py` and `tests/test_jobs_handlers.py` pass unchanged.)

## 3. Verify Shared Execution Paths

- [x] 3.1 Add or update command and schedule tests proving manual and scheduled full/incremental jobs invoke the same command with equivalent context; verify both backup types and both invocation sources are covered. (Added `tests/test_backup_dispatch_parity.py` with two end-to-end tests — one per backup type — that patch `JOB_HANDLERS` to capture what manual API submission and triggered-schedule submission each pass through, and assert equivalent cluster/group/repository/type-specific-option context. Also added `test_run_due_schedules_triggers_due_incremental_schedule_with_equivalent_context` to `tests/test_commands_schedules.py`, mirroring the existing `backup_full` schedule-dispatch test for `backup_incremental` — previously only `backup_full` was covered on the schedule side.)
- [x] 3.2 Run the relevant API, schedule, job, and backup command test suites and confirm they pass. (`pytest tests/test_backup_dispatch_parity.py tests/test_commands_schedules.py tests/test_api_schedules.py tests/test_api_jobs.py tests/test_commands_backup.py tests/test_jobs_handlers.py` — all pass. Full suite also run; see apply-session notes.)
