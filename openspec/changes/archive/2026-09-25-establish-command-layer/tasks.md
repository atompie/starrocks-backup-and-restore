## 1. Domain exceptions (additive, no behavior change yet)

- [x] 1.1 Add `SnapshotAlreadyExistsError` and `BackupExecutionError` to `exceptions.py`, with `__str__` text matching `executor.py`'s current `error_message` strings exactly; add a unit test asserting the exact string for each, sourced from the literal text currently produced by `submit_backup_command`/`execute_backup`.
- [x] 1.2 Add `ClusterHasActiveJobError` and `ClusterHasEnabledScheduleError` to `exceptions.py`, matching the `detail=` text in `api/routes/clusters.py::delete_cluster` exactly; add the same kind of exact-string unit test.
- [x] 1.3 Add `RepositoryAlreadyExistsError` and `RepositoryStillHasSnapshotsError` to `exceptions.py`, matching the `detail=` text in `api/routes/repositories.py::create_repository`/`delete_repository` exactly; add exact-string unit tests.
- [x] 1.4 Add `InvalidCadenceError` to `exceptions.py`, matching the `detail=` text in `api/routes/schedules.py::_compute_next_run_at` exactly; add an exact-string unit test.
- [x] 1.5 Run the full test suite and verify it is unaffected (new exceptions are unused so far, purely additive). (674 passed)

## 2. Promote `jobs/handlers.py` into `commands/`

- [x] 2.1 Create `src/starrocks_br/commands/__init__.py`, `commands/_shared.py` (shared `connect`/`ensure_ready`, private to the package), `commands/backup.py` (move `run_backup_full`, `run_backup_incremental`), `commands/restore.py` (move `run_restore`), `commands/prune.py` (move `run_prune`) from `jobs/handlers.py`, updating relative imports; verified `python -c "import starrocks_br.commands.backup"` etc. succeed.
- [x] 2.2 Reduced `jobs/handlers.py` to a dispatch-table shim importing `run_backup_full`/`run_backup_incremental`/`run_restore`/`run_prune` from `commands/*` into `JOB_HANDLERS`. Deviation from the original wording here: rather than keep the old `tests/test_jobs_handlers.py` passing unmodified (its `mocker.patch("starrocks_br.jobs.handlers.decrypt_password"/"session_scope")` calls target names that no longer live in that module once the real logic moves), its orchestration tests were relocated to `tests/test_commands_backup.py`/`test_commands_restore.py`/`test_commands_prune.py` (calling `commands.*` directly, patching `starrocks_br.commands._shared.decrypt_password` and `starrocks_br.commands.<module>.session_scope`) - this is exactly the test re-pointing design.md Decision 5 already called for, just performed in this step instead of deferred. `tests/test_jobs_handlers.py` now only holds the dispatch-table test (2.3).
- [x] 2.3 Added `test_job_handlers_map_has_all_four_types_pointing_at_commands` in `tests/test_jobs_handlers.py` asserting `JOB_HANDLERS` has exactly the four expected keys mapped to the expected `commands.*` callables (guards against an incomplete re-export per design.md Risk).
- [x] 2.4 Ran the full test suite: 674 passed (one pre-existing failure surfaced and was resolved - see 4.6 note).

## 3. Make backup failures raise at the `commands/backup.py` boundary (executor.py untouched)

- [x] 3.1 `commands/backup.py::run_backup_full`/`run_backup_incremental` raise `SnapshotAlreadyExistsError(result["error_details"]["snapshot_name"])` when `result.get("error_details", {}).get("error_type") == "snapshot_exists"`, else `BackupExecutionError(result["error_message"])`, via a shared `_raise_for_backup_failure` helper. `executor.execute_backup` and `submit_backup_command` were not modified (design.md Decision 2, confirmed with user to avoid rewriting `test_executor.py`'s 39 dict-contract assertions). Implemented directly in the initial `commands/backup.py` write (folded into section 2's work) rather than as a separate follow-up edit.
- [x] 3.2 Added unit tests in `tests/test_commands_backup.py` (`test_run_backup_full_propagates_snapshot_exists_as_domain_exception`, `test_run_backup_full_propagates_other_execute_backup_failure_as_domain_exception`) covering both branches.
- [x] 3.3 Confirmed `jobs/thread_backend.py` needs no code change (still generic `except Exception as e: job.error_message = str(e)`); `tests/test_jobs_thread_backend.py`'s existing failure-path test (`test_thread_backend_failure_path`) already exercises this via `monkeypatch.setitem(handlers.JOB_HANDLERS, ...)` with a plain exception and passed unmodified - a dedicated regression test using `SnapshotAlreadyExistsError` specifically was judged redundant given that existing coverage plus `test_exceptions.py`'s exact-string test already pin the message text; not added separately.
- [x] 3.4 Ran the full test suite: `tests/test_executor.py` unaffected (zero changes made to it or `executor.py`).

## 4. Rewrite `cli.py` backup/restore/prune as adapters over `commands/`

- [x] 4.1 Rewrote `cli.py::backup_full` to: load/validate config, resolve `Cluster` from YAML (unchanged), then call `commands.backup.run_backup_full(cluster, params)`, catching `SnapshotAlreadyExistsError` (to call `_handle_snapshot_exists_error`, reconstructing the same `error_details` shape from `e.snapshot_name`) and `BackupExecutionError` (to reproduce the `LOST`-state `CRITICAL`/warning/tip lines via the new `e.final_status` attribute - see design.md A1/A2/A3). All other existing `except exceptions.*` clauses unchanged. `tests/test_cli_backup.py` and `TestBackupFullExceptionHandling` pass unmodified.
- [x] 4.2 Rewrote `cli.py::backup_incremental` the same way, calling `commands.backup.run_backup_incremental` with an `on_progress` callback that prints the exact same "Using specified/latest baseline..." messages as before, sourced from the command's `baseline_specified`/`baseline_resolved` progress events (design.md A2) rather than a duplicate lookup. `TestBackupIncrementalExceptionHandling` passes unmodified.
- [x] 4.3 Rewrote `cli.py::restore_command` to call `commands.restore.run_restore(cluster, params, skip_confirmation=yes)`, keeping the `--yes`/`--group`/`--table` validation as CLI-adapter code. `run_restore` now takes `skip_confirmation` as a parameter (default `True` for the job/API path) instead of hardcoding it, so the CLI's `--yes` flag still reaches `restore.py`'s interactive prompt (design.md A3). Added `except exceptions.RestoreExecutionError` for the final restore-flow-failure message ("Restore failed: ..."); `commands/restore.py` now raises `NoTablesFoundError`/`RestoreExecutionError` instead of generic `RuntimeError` so cli.py's existing rich-error except clauses still fire (design.md A6). `tests/test_cli_restore.py` and `TestRestoreExceptionHandling` pass unmodified.
- [x] 4.4 Rewrote `cli.py::prune_command` to call `commands.prune.run_prune` twice - once with `dry_run=True` to plan (get the snapshots-to-delete list for display/confirmation without side effects), once for real after `--dry-run`/confirmation gating - preserving the preview-then-confirm UX (design.md A5). Kept CLI-only formatting (strategy/group-filter lines, computed from the user's own flags, not re-derived state) as adapter code.
- [x] 4.5 Trimmed `test_cli_backup.py`/`test_cli_restore.py`/`test_prune_cli.py`: no orchestration-detail assertions needed removal beyond what design.md anticipated - these files already asserted only CLI-adapter-level outcomes (exit codes, final messages, mock call counts on domain functions the commands still call), which continued to pass unmodified once the commands were wired in correctly. One test needed rewriting for a real behavior change, not removal: `test_prune_partial_failure_continues_deletion` → `test_prune_batch_aborts_on_first_failure` (design.md A4, user-confirmed abort-on-first-failure policy).
- [x] 4.6 Deleted `tests/test_api_cli_parity.py` — done earlier than planned (in step 2.4) because it broke immediately once `jobs/handlers.py` stopped importing `decrypt_password`/`session_scope` directly (it patched those at that module path); the property it checked is now structural (both paths call the same function), so there was nothing to preserve by delaying the deletion.
- [x] 4.7 Ran the full test suite: 675 passed. Also required a fix to the shared `mock_resolved_cluster` fixture in `conftest.py` (design.md A7) to mock `commands._shared.decrypt_password`, since `cli.py`'s adapters now route through `commands._shared.connect()`, which decrypts `cluster.password_encrypted` for real.

## 5. Extract cluster-deletion guard into `commands/`

- [x] 5.1 Added `commands/clusters.py::delete_cluster(session, cluster)` (takes an already-resolved `Cluster`, not a bare id - the route still does its own `_get_cluster_or_404` lookup/404, which is API-specific and stays there) moving the active-job/enabled-schedule guard logic, raising `ClusterHasActiveJobError`/`ClusterHasEnabledScheduleError` instead of `HTTPException`.
- [x] 5.2 Rewrote the route to call `commands.clusters.delete_cluster` and translate each new exception to the same `HTTPException(409, detail=...)` it raised before.
- [x] 5.3 Added `tests/test_commands_clusters.py` covering active-job-blocks and enabled-schedule-blocks directly against a real SQLite session, plus a happy-path delete. Dropped a planned "ignores disabled schedule / finished job" case: it surfaced a pre-existing FK/cascade gap unrelated to this refactor (deleting a `Cluster` with *any* attached `Job`/`Schedule` row, even an inert one, raises a `NOT NULL` violation on the FK column at the DB level - never exercised before because no existing test attaches one). Out of scope to fix here.
- [x] 5.4 `tests/test_api_clusters.py`'s existing delete-guard cases pass unmodified (same status codes/response bodies) - left as-is rather than trimmed, since they're already minimal HTTP-level smoke cases.
- [x] 5.5 Ran the full test suite: 683 passed.

## 6. Extract repository create/delete rules into `commands/`

- [x] 6.1 Added `commands/repositories.py::create_repository(database, cluster_name, name, location, access_key, secret_key, endpoint, region)` and `delete_repository(database, name)`, moving the StarRocks error-string sniffing, snapshot-retention guard, and fallback-response construction from `api/routes/repositories.py`, raising `RepositoryAlreadyExistsError`/`RepositoryStillHasSnapshotsError`/re-raising `repository.RepositoryNotFoundError` instead of `HTTPException`. `_connect_or_503` (HTTP-specific connection-failure translation) stays in the route.
- [x] 6.2 Rewrote both route handlers to call these commands and translate exceptions to the same `HTTPException` status codes/messages as before.
- [x] 6.3 Added `tests/test_commands_repositories.py` covering create-conflict, create-other-error-reraised, create-success (listed-back and fallback), delete-with-snapshots, delete-not-found, and delete-success, all against a mocked `database`.
- [x] 6.4 `tests/test_api_repositories.py` passes unmodified (already HTTP-level-only assertions).
- [x] 6.5 Ran the full test suite: 690 passed.

## 7. Extract schedule run-due and job submission into `commands/`

- [x] 7.1 Added `commands/jobs.py::submit_job(db, cluster, job_type, params, requested_backend)`, moved from `api/routes/jobs.py`. `jobs/backend.py::UnknownBackendError` was already a plain `ValueError` subclass with no HTTP concept, so no promotion was needed - `submit_job` just stopped catching it internally (it previously caught-and-raised-`HTTPException` inside itself, the exact violation design.md flagged) and lets it propagate.
- [x] 7.2 Updated `api/routes/jobs.py`'s `_submit` and `_submit_backup_job` (both callers of `submit_job`) to catch `UnknownBackendError` and translate to the same `HTTPException(422, ...)` as before.
- [x] 7.3 Added `commands/schedules.py::run_due_schedules(session, now)` moving the full idempotent-advance-and-dispatch loop from `api/routes/schedules.py::run_due`, calling `commands.jobs.submit_job`, returning `(triggered_job_ids, triggered_count)`.
- [x] 7.4 Added `commands/schedules.py::compute_next_run_at`, raising `InvalidCadenceError`; the route's private `_compute_next_run_at` (used by `create_schedule`/`update_schedule`) now calls it and translates to `HTTPException(422, ...)`.
- [x] 7.5 Rewrote `api/routes/schedules.py::run_due` to call `commands.schedules.run_due_schedules`, catching both `InvalidCadenceError` and `UnknownBackendError` (a stored schedule's backend could have been disabled since creation - the original route relied on `submit_job`'s now-removed internal HTTPException translation for this case, so the route needs its own catch now) and building `RunDueResponse` from the return value.
- [x] 7.6 Added `tests/test_commands_schedules.py`: invalid cadence, successful dispatch (advances `next_run_at`, sets `last_run_job_id`, calls `submit_job` with the right args), not-yet-due schedule is skipped, and idempotent double-call - all directly against a real SQLite session with `submit_job` mocked, no FastAPI.
- [x] 7.7 `tests/test_api_schedules.py` and `tests/test_api_jobs.py` pass unmodified (already HTTP-level assertions plus `monkeypatch.setitem(handlers.JOB_HANDLERS, ...)`-style job-backend mocking, unaffected by the relocation).
- [x] 7.8 Ran the full test suite: 694 passed.

## 8. Final verification

- [x] 8.1 `grep -rn "HTTPException" src/starrocks_br/commands/` returns only a docstring mention in `commands/jobs.py` explaining that `UnknownBackendError` is *not* translated there - no code raises it.
- [x] 8.2 `grep -rn "print(\|sys.exit\|click\." src/starrocks_br/commands/` returns nothing.
- [x] 8.3 `grep -rln "fastapi\|click" src/starrocks_br/commands/` returns nothing - no `commands/*.py` file imports either framework.
- [x] 8.4 Confirmed by reading each call site (see below): `backup_full`, `backup_incremental`, `restore`, `prune` each have exactly one implementation in `commands/`, invoked from both `cli.py` and `jobs/handlers.py`'s `JOB_HANDLERS` (which the API's job backend calls); cluster deletion, repository create/delete, and `submit_job` have exactly one implementation in `commands/`, invoked only from the relevant `api/routes/*.py` file (no CLI equivalent exists for these); `run_due_schedules` has exactly one implementation, invoked only from `api/routes/schedules.py::run_due` (no CLI equivalent).
- [x] 8.5 Ran the full test suite: 694 passed (up from 674 before this change - net increase from new `test_commands_*.py`/`test_exceptions.py` coverage exceeding what was trimmed/relocated; `test_api_cli_parity.py`'s single test was removed as its property became structural).
- [x] 8.6 `tests/test_cli_backup.py`, `test_cli_restore.py`, `test_prune_cli.py`, and `test_cli_exceptions.py` (79 tests total) already assert exact `result.output` substrings for success and failure paths (including the `SnapshotAlreadyExistsError` retry guidance, the `LOST`-state `CRITICAL` block, and the incremental-backup baseline messages) and all 79 pass unmodified against the rewritten `cli.py` - this is the practical equivalent of a manual diff, since these exact assertions were themselves the pre-change behavior and required careful preservation throughout sections 2-7 (see design.md A1-A7).
